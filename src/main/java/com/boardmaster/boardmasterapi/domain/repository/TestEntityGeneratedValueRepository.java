package com.boardmaster.boardmasterapi.domain.repository;

import com.boardmaster.boardmasterapi.domain.TestEntityGeneratedValue;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TestEntityGeneratedValueRepository extends JpaRepository<TestEntityGeneratedValue, Long> {
}
